name: resize_rotate_image_example
description: An example project for the resize_rotate_image package.
version: 1.0.0+1 

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  resize_rotate_image: 
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/images/